
This tool integrates an own scripting language and makes use out of it.

Idea : .bline scripts


Don´t you worry about not knowing about how to get started. We got an perfectly adjusted growing Wiki for you to read through.

recon 
    nmap
    ...



recon[ip=x, url, emails, usernames...] -> 
find_exploit=[all] 
    if(os=windows!version)
    then: 
        stop 
else: 
    exploit ->
    shell -> 
    clean 
    
&&  

if wireshark[alert] 
then: stealth + 

recon[ip=x, url, emails, usernames...] <enter>




recon[ip=x, url, emails, usernames] -> <enter>



?when() 

| (grep (zsh) ??? tbd)

&& (parallel)

& (in background)

-> 


load long term 
use a modules .bline 
run : only run no other pupose 

1. wrote command in the cmd 


a;ldkfja;ldkjfa;kldf 

> run 


2. use .bline once 

use <path> 

run 

3. use .bline long term

load <path>

use list 

use <number>

run 


4. use more then one .bline file

list 

1. blabla.line
2. 2.bline 

use 1 2 

/home/who/... goon.bline

use /home/who/... goon.bline

run  /home/who/... goon.bline
run 

// 1 time use 

~/db/.... 

load /home/who/... goon.bline


edit test.bline

That file... do you want to create it? 


if(windows!version) -> try_exploit(all)



-----------------------------------------
delete /path/to/.bline
edit /path/to/.bline
-----------------------------------------

