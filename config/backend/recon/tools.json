{
  "nmap": {
    "action": "recon",
    "targets": ["ip", "domain"],
    "supports": ["ports", "stealth", "intensity", "discovery"],
    "priority": 10
  },

  "curl_probe": {
    "action": "curl_probe",
    "targets": ["ip", "domain", "url"],
    "supports": ["protocol"],
    "priority": 10
  }
}
